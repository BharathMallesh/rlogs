[package]
name = "rlog-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
crossbeam-channel = "0.5"
lazy_static = "1.4"
quick-xml = "0.31"
tracing-appender = "0.2"
jni = "0.21"
rolling-file = "0.2"
regex = "1"
chrono = { version = "0.4", features = ["std"] }
